CI/CD 是一种自动化的软件交付流程。  

```
CI = 自动构建和检查代码
CD = 自动部署代码
```

CI（持续集成）指的是开发者提交代码后，系统自动执行代码检查、测试和构建，保证代码质量。  
CD（持续交付/持续部署）是在 CI 通过后自动完成打包和部署流程，使代码能够快速上线。  ()
在前端项目中通常会使用 GitHub Actions 或 GitLab CI 来实现自动化构建和部署。