### 浮动的“半脱离”状态

虽然我们常说浮动脱离了文档流，但它与 `absolute` 的“完全消失”不同，它被称为**“半脱离”**：

- **对块级元素来说**：它是**脱离**的。原本排在它后面的块级元素会无视它的存在，直接占据它原有的位置（也就是你之前看到的“钻到下面去”）。
    
- **对行内元素（文字/图片）来说**：它又是**不脱离**的。文字能“看”到浮动元素，并会自动避开它，形成环绕效果。