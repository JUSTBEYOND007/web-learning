```css
flex-grow: 1;   // 可以分配剩余空间，按比例增长
flex-shrink: 1; // 空间不足时允许缩小
flex-basis: 0%; // 初始占用空间为 0
``` 


`flex-grow` 表示 **当容器有剩余空间时，元素的放大比例**。  
`flex-shrink` 表示 **当容器空间不足时，元素的缩小比例**。  
`flex-basis` 表示 **元素在主轴上的初始大小**。