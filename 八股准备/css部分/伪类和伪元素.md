伪类（Pseudo-class）是用来选择元素在某些**状态**下的样式，比如鼠标悬停、获得焦点、链接访问、表单校验状态等。
伪元素用于选中元素的一部分或给元素创建一个虚拟节点，比如 `::before` 和 `::after` 用来生成内容，`::first-letter`、`::first-line` 用来选择文本的一部分。


伪类用于描述元素在特定状态下的表现
伪元素用于描述元素的某一部分或生成的虚拟内容，

![Pasted image 20260309100716](../../assets/images/Pasted%20image%2020260309100716.png)