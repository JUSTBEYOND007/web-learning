CSR 是浏览器端渲染，页面内容由 JS 动态生成，首屏慢但交互快。  
SSR 是服务器端动态生成 HTML，首屏快、SEO 好，但服务器压力较大。  
SSG 是构建时生成静态 HTML，性能最好但不适合频繁变更的数据。



SPA 方式：

点击菜单 → JS 改变 router → 切换组件 → 页面瞬间变化

没有刷新，没有白屏，没有重新请求 HTML。
更流畅的用户体验：页面不需要重新加载，通过 AJAX 更新内容，用户体验更接近原生应用。
b
前后端分离：前端和后端可以独立开发、部署，提高开发效率。