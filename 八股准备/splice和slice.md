**splice 返回的不是修改后的原数组，而是“被删除的元素组成的新数组”**
![[Pasted image 20260227101004.png]]


### splice 的基本语法

array.splice(start, deleteCount, item1, item2, ...)

- **start**：开始操作的索引
    
- **deleteCount**：要删除的元素个数
    
- **item1, item2 …**：可选，要插入的新元素