### **`todoSlice.actions` 是动作生成器（给 dispatch 用）**

### **`todoSlice.reducer` 是更新 state 的函数（给 store 用）**

  
TypeScript

```
const 变量名 : 它的类型 = 它的值;
```

- **dispatch(action)** → 触发 reducer
    
- **action creator** 生成 action
    
- **reducer** 根据 action 更新 state