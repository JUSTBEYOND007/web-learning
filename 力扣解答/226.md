我们就是深度递归就好了 先处理子节点子树 再处理当前左右子树

```js
var invertTree = function(root) {
	
    if(!root)

        return null

    let left=invertTree(root.left)

    let right=invertTree(root.right)

    root.left=right

    root.right=left

    return root

};
```