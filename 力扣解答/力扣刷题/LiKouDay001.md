# 反转链表
利用双指针  start、end
`while(start<end)`
对于奇数个、偶数个 结果都一样

# 在 Java 中，`length()` 和 `length` 的使用场景
- ### **`length` 作为属性**
	适用于数组**：对于所有数组类型（包括 `int[]`、`char[]`、`String[]` 等），使用 `length` 属性来获取数组的长度
- 对于字符串类型 (`String`)，使用 `length()` 方法来获取字符串中字符的数量