基本类型使用     typeof value === '类型字符串'```
`typeof` 可以准确判断：`string`, `number`, `boolean`, `undefined`, `symbol`, `bigint`, `function`
 ```
  typeof obj!=='object'
 ```

引用数据类型
a  instanceof b 返回的是Boolean类型
```
obj instanceof RegExp
```
