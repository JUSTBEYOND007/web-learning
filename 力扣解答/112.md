```js
var hasPathSum = function(root, targetSum) {

    function dfs(node,target){

        if(node===null)

            return false//先判断是否为null 是为了防止根节点为null

        if(node.left===null&&node.right===null){//判断是否为叶子节点

            if(node.val===target)

                return true

            return false

        }

        return dfs(node.left,target-node.val)||dfs(node.right,target-node.val)
		//用|| 有一个为true就可以
    }

    return dfs(root,targetSum)

};
```

出现RangeError: Maximum call stack size exceeded