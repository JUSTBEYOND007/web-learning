逻辑
当前最上面的一行。遍历结束后执行 top++  注意是top++

后两个循环 需要加上if判断 防止**防止单行（或奇数行）矩阵在中心重叠时，重复打印中间那一行**和**防止单列（或奇数列）矩阵在中心重叠时，重复打印中间那一列**

![[Pasted image 20260115145517.png]]

```
var spiralOrder = function(matrix) {

    let left=0,right=matrix[0].length-1,top=0,bottom=matrix.length-1

    const res=[]

    while(left<=right&&top<=bottom){

        for(let i=left;i<=right;i++){

            res.push(matrix[top][i])

        }

        top++

        for(let i=top;i<=bottom;i++){

            res.push(matrix[i][right])

        }

        right--

        if(top<=bottom){//重点 再次判断 如果top>bottom 即已经从左到右遍历过了  不能再从右向左再重复遍历一次了

            for(let i=right;i>=left;i--){

                res.push(matrix[bottom][i])

            }

            bottom--

        }

            if(left<=right){

                for(let i=bottom;i>=top;i--){

                res.push(matrix[i][left])

            }

            left++    

        }

    }

  

    return res

};
```