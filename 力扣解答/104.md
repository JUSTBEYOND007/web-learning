//从下向上 
深度遍历 if来判断底层  即可 不需要while循环的

我们不需要保存子节点的值 不需要闭包 如果为null则depth为0
节点深度为子节点高度最大值+1
```Js
var maxDepth = function(root) {

        if(!root){

            return 0

        }

        return Math.max(maxDepth(root.left),maxDepth(root.right))+1

};
	```