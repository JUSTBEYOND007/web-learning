### 时间超时问题
大概率是 循环处理 问题 死循环
我们应该从循环条件入手
例如
  while(queue.length>=0){}
  这里queue.length>=0肯定是死循环呀 为0也执行肯定不对
 应该是queue.length>0

throw new TypeError(__serialize__(ret) + " is not valid value for the expected return type list<integer>");
这个问题是我们返回值问题

例如我们返回Null
但是题目要求返回的是 the expected return type list<integer>");  所以我们应该返回[]