完全背包问题
自己需要再看
[[322]] 一样的题 判断条件不同
```js
var numSquares = function(n) {

    const dp=new Array(n+1).fill(Number.MAX_VALUE)

    dp[0]=0

    for(let i=1;i<=n;i++){

        for(let j=1;j*j<=i;j++){

            dp[i]=Math.min(dp[i-j*j]+1,dp[i])

        }

    }

    return dp[n]

};
```