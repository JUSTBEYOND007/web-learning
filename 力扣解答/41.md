缺失的那个数，一定在 [1,N+1]  区间


** 这个闭区间内。
![Pasted image 20260107220932](../assets/images/Pasted%20image%2020260107220932.png)

我们注意到我们要记录的数字范围是1到N 而数组是从0~N-1 我们可以使用该数组逻辑关系下表i来记录i+1的值来确定对应关系

```
var firstMissingPositive = function(nums) {

    for(let i=0;i<nums.length;i++){

        while(nums[i]>=1&&nums[i]<=nums.length&&nums[i]!=nums[nums[i]-1]){

           [nums[nums[i]-1],nums[i]] = [nums[i],nums[nums[i]-1]]

        }

    }

    for(let i=0;i<nums.length;i++){

        if(nums[i]!=(i+1))

            return i+1

    }

    return nums.length+1

};
```