*关键词   **静态类型检查减少bug** 提高代码可读性  开发体验（自动补全、类型提示）提高代码的可维护性。 *
```
在项目中使用 TypeScript 主要有几个优点：

第一，TypeScript 提供静态类型检查，可以在编译阶段发现错误，减少运行时 bug。
第二，类型定义让代码结构更加清晰，提高代码可读性。
第三，IDE 可以提供更好的开发体验，比如自动补全、类型提示和跳转定义。
第四，在大型项目中可以规范数据结构，提高代码的可维护性。
第五，TypeScript 更方便进行代码重构，修改类型后可以在编译阶段发现问题。
```