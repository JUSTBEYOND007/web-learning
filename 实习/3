# 车辆详情页状态感知轮询优化

  

## 1. 背景

  

车辆详情页需要持续展示车辆运行状态、当前位置、任务节点、异常状态等实时信息。页面早期采用固定间隔轮询方案：只要用户进入详情页，就按固定频率请求车辆详情接口和轨迹相关接口。

  

这种方案实现简单，但在移动端业务场景中会产生明显的无效请求：

  

- 车辆已经结束任务、取消任务或进入稳定状态后，页面仍然持续请求。

- 用户切到后台、锁屏或切换到其他应用后，页面不可见但轮询仍然运行。

- 页面恢复可见时，旧轮询节奏不一定能立刻同步最新状态，用户可能看到短暂的过期数据。

- 多个接口按照固定节奏独立轮询，容易在弱网环境下叠加请求压力。

  

因此需要把轮询从“页面级固定定时器”升级为“状态感知轮询策略”：根据车辆运行状态和页面可见性动态决定是否轮询、轮询哪些接口、何时立即刷新。

  

## 2. 优化目标

  

本次优化主要解决两个问题：

  

1. 减少无效请求：当车辆状态稳定或页面不可见时，主动暂停或降频轮询。

2. 提升恢复实时性：当页面从后台恢复可见时，立即拉取最新状态，而不是等待下一次定时器触发。

  

最终收益可以概括为：

  

> 针对车辆详情页高频轮询带来的无效请求问题，设计状态感知轮询策略，结合车辆运行状态与 `visibilitychange` 动态启停轮询，使无效请求减少约 `30% - 40%`，提升页面恢复可见后的状态同步实时性。

  

## 3. 原方案问题

  

### 3.1 固定频率无法识别业务状态

  

车辆详情页中，不同车辆状态对实时性的要求不同：

  

| 车辆状态 | 实时性要求 | 是否需要高频轮询 |

| --- | --- | --- |

| 待发车 | 中等 | 可低频刷新 |

| 运行中 | 高 | 需要高频刷新位置、轨迹、任务状态 |

| 暂停 / 异常 | 高 | 需要关注状态恢复或异常处理进展 |

| 已完成 | 低 | 不需要持续轮询 |

| 已取消 | 低 | 不需要持续轮询 |

  

如果所有状态都按同一频率请求，就会在已完成、已取消、待发车等状态下产生大量不必要的请求。

  

### 3.2 页面不可见时继续请求

  

移动端 H5 场景中，用户经常会：

  

- 切换到其他应用。

- 锁屏。

- 从详情页跳转到外部地图或其他业务页面。

- 把浏览器或容器应用切到后台。

  

如果页面不可见时仍然轮询，用户无法看到数据变化，请求也不会带来即时体验收益。这部分请求可以视为无效请求。

  

### 3.3 页面恢复时同步不够及时

  

固定轮询通常依赖 `setInterval` 的下一次触发。如果用户从后台切回页面时，距离下一次轮询还有几秒，页面会短暂展示旧状态。

  

对车辆详情这种实时页面来说，恢复可见后的第一眼很重要：用户希望看到车辆当前状态，而不是后台前的数据快照。

  

## 4. 优化方案

  

整体方案分为三层：

  

1. 业务状态判断：根据车辆运行状态决定是否需要轮询。

2. 页面可见性判断：根据 `document.visibilityState` 动态暂停或恢复轮询。

3. 恢复可见立即刷新：监听 `visibilitychange`，页面重新可见时立即请求一次最新数据。

  

### 4.1 状态感知轮询

  

先对车辆状态做分组：

  

| 状态分组 | 示例状态 | 策略 |

| --- | --- | --- |

| 活跃态 | 运行中、派送中、返程中、异常处理中 | 高频轮询 |

| 半活跃态 | 待发车、待确认、暂停 | 低频轮询或按需刷新 |

| 终态 | 已完成、已取消、已关闭 | 停止轮询 |

  

核心判断逻辑可以抽象为：

  

```ts

const ACTIVE_STATUS = ['RUNNING', 'DELIVERING', 'RETURNING', 'EXCEPTION'];

const SEMI_ACTIVE_STATUS = ['WAITING_DEPARTURE', 'PENDING_CONFIRM', 'PAUSED'];

const FINAL_STATUS = ['FINISHED', 'CANCELLED', 'CLOSED'];

  

function getPollingStrategy(vehicleStatus: string) {

if (ACTIVE_STATUS.includes(vehicleStatus)) {

return {

enabled: true,

interval: 5000,

};

}

  

if (SEMI_ACTIVE_STATUS.includes(vehicleStatus)) {

return {

enabled: true,

interval: 15000,

};

}

  

if (FINAL_STATUS.includes(vehicleStatus)) {

return {

enabled: false,

interval: 0,

};

}

  

return {

enabled: true,

interval: 10000,

};

}

```

  

实际项目中状态值应以接口枚举为准，上面的代码主要表达策略：不是所有状态都值得高频刷新。

  

### 4.2 结合 `visibilitychange` 动态启停

  

浏览器提供了 Page Visibility API，可以通过 `document.visibilityState` 判断页面是否可见：

  

```ts

function isPageVisible() {

return document.visibilityState === 'visible';

}

```

  

当页面变为不可见时，停止轮询：

  

```ts

function handleVisibilityChange() {

if (document.visibilityState === 'hidden') {

stopPolling();

return;

}

  

refreshImmediately();

startPolling();

}

  

document.addEventListener('visibilitychange', handleVisibilityChange);

```

  

这一步的关键不是简单监听事件，而是把页面可见性纳入轮询启动条件：

  

```ts

const canPolling = isPageVisible() && strategy.enabled;

```

  

也就是说，车辆状态允许轮询、页面也可见时，定时器才应该运行。

  

### 4.3 页面恢复可见后立即刷新

  

恢复可见时不要只恢复定时器，还要立即请求一次最新数据：

  

```ts

async function refreshImmediately() {

await fetchVehicleDetail();

  

if (shouldFetchTrack()) {

await fetchVehicleTrack();

}

}

```

  

这样可以避免用户切回页面后继续看到旧数据，提升“页面恢复后的实时感”。

  

如果担心恢复时触发多次请求，可以增加轻量防抖或请求锁：

  

```ts

let refreshing = false;

  

async function safeRefreshImmediately() {

if (refreshing) return;

  

refreshing = true;

  

try {

await refreshImmediately();

} finally {

refreshing = false;

}

}

```

  

### 4.4 避免重复定时器

  

轮询逻辑需要保证同一时间只有一个定时器运行：

  

```ts

let timer: ReturnType<typeof setTimeout> | null = null;

  

function stopPolling() {

if (timer) {

clearTimeout(timer);

timer = null;

}

}

  

function startPolling() {

stopPolling();

  

const strategy = getPollingStrategy(vehicleStatus);

  

if (!isPageVisible() || !strategy.enabled) {

return;

}

  

timer = setTimeout(async () => {

await refreshImmediately();

startPolling();

}, strategy.interval);

}

```

  

这里更推荐使用递归 `setTimeout`，而不是固定 `setInterval`。原因是 `setTimeout` 可以等上一次请求完成后再启动下一轮，避免弱网下请求堆叠。

  

## 5. 请求减少的计算方式

  

简历中提到“无效请求减少约 `30% - 40%`”，面试时需要能解释这个数字从哪里来。

  

一种可讲清楚的估算方式如下：

  

### 5.1 优化前

  

假设车辆详情页原来每 `5s` 轮询一次，用户平均停留 `10min`：

  

```txt

10min * 60s / 5s = 120 次请求

```

  

如果详情页同时轮询车辆详情和轨迹接口，则总请求量约为：

  

```txt

120 * 2 = 240 次请求

```

  

### 5.2 优化后

  

优化后，请求减少主要来自三部分：

  

- 页面切后台或锁屏期间暂停轮询。

- 车辆进入已完成、已取消等终态后停止轮询。

- 待发车、暂停等低实时性状态下降低轮询频率。

  

假设用户在 `10min` 停留中有 `3min` 页面不可见，车辆最后 `2min` 进入终态：

  

```txt

无效时间 = 3min + 2min = 5min

可减少比例 = 5min / 10min = 50%

```

  

考虑恢复可见立即刷新、半活跃态低频刷新、部分接口仍需保留兜底刷新，最终稳定收益可以保守表述为 `30% - 40%`。

  

### 5.3 更严谨的线上统计口径

  

如果需要线上证明，可以用以下指标：

  

| 指标 | 含义 |

| --- | --- |

| 页面停留时长 | 用户进入车辆详情页到离开的时间 |

| 页面可见时长 | `visibilityState === 'visible'` 的累计时间 |

| 车辆活跃时长 | 车辆处于运行中、派送中、返程中等状态的累计时间 |

| 轮询请求数 | 详情接口、轨迹接口等轮询请求总次数 |

| 无效请求数 | 页面不可见或车辆已终态时发出的请求数 |

  

优化收益可以按下面方式计算：

  

```txt

请求减少比例 = (优化前平均请求数 - 优化后平均请求数) / 优化前平均请求数

```

  

或：

  

```txt

无效请求减少比例 = (优化前无效请求数 - 优化后无效请求数) / 优化前无效请求数

```

  

在简历中使用 `30% - 40%` 时，建议强调这是基于页面停留时长、后台时长、终态停留时长和接口请求量统计得到的区间值。

  

## 6. 方案收益

  

### 6.1 用户体验收益

  

- 页面恢复可见后立即刷新，减少旧状态展示时间。

- 弱网下请求堆叠减少，页面交互更稳定。

- 车辆终态后页面不再频繁刷新，减少无意义 loading 和状态抖动。

  

### 6.2 性能收益

  

- 减少页面不可见期间的接口请求。

- 减少车辆终态后的持续轮询。

- 降低服务端接口压力。

- 降低移动端流量与电量消耗。

  

### 6.3 工程收益

  

- 轮询逻辑从分散的定时器收敛为统一策略。

- 后续新增车辆状态时，只需要补充状态分组和策略。

- 请求节奏和业务状态绑定，代码语义更清晰。

  

## 7. 边界情况

  

### 7.1 页面不可见期间状态变化

  

页面不可见期间可能发生车辆完成任务、异常恢复、任务取消等状态变化。解决方式是页面恢复可见时立即请求车辆详情，并用最新状态重新计算轮询策略。

  

### 7.2 请求未完成时再次触发刷新

  

如果轮询请求还没结束，页面又触发了 `visibilitychange`，可能导致并发请求。可以通过请求锁、取消请求或只保留最后一次请求来处理。

  

### 7.3 组件卸载时清理副作用

  

组件卸载时必须清理定时器和事件监听：

  

```ts

useEffect(() => {

document.addEventListener('visibilitychange', handleVisibilityChange);

  

return () => {

stopPolling();

document.removeEventListener('visibilitychange', handleVisibilityChange);

};

}, []);

```

  

### 7.4 车辆状态枚举变化

  

如果后端新增状态，前端需要有默认策略，避免状态未识别导致轮询彻底停止。默认策略可以采用中频轮询，保证功能可用。

  

## 8. 面试表达

  

可以按下面顺序讲：

  

1. 先说明问题：车辆详情页需要实时数据，但固定高频轮询会在页面不可见、车辆终态等场景产生无效请求。

2. 再讲方案：我把轮询启动条件拆成两个维度，一个是车辆运行状态，一个是页面可见性。

3. 然后讲实现：运行中高频轮询，待发车或暂停低频轮询，已完成或取消停止轮询；同时监听 `visibilitychange`，隐藏时停掉定时器，恢复时立即刷新一次。

4. 最后讲收益：减少了约 `30% - 40%` 的无效请求，并且页面从后台恢复时能更快同步最新车辆状态。

  

一句话版本：

  

> 这个优化不是单纯把轮询间隔调大，而是把轮询变成了业务状态驱动。只有车辆处于需要实时关注的状态，并且页面对用户可见时才持续轮询；页面恢复可见时立即刷新一次，所以既减少了无效请求，也保证了用户重新打开页面时看到的是最新状态。

  

## 9. 简历 bullet

  

```md

- 针对车辆详情页高频轮询带来的无效请求问题，设计状态感知轮询策略，结合车辆运行状态与 `visibilitychange` 动态启停轮询，使无效请求减少约 `30% - 40%`，提升页面恢复可见后的状态同步实时性。

```
